import React from 'react';

const PricingPanel = () => <div>PRICING PANEL</div>;

export default PricingPanel;

// Here to build a seperate UI, to be run in parralel with Basket on different Route or split App view for Basket and PricingPanel
// To read Product and Pricin information from JSON to begin with (NODE)
// To change in fields prices, add new items, change pricing rules
// Once chaged to be saved, which will trogger saving a new JSON (NODE)
// This new JSON will be consumed by Basket
